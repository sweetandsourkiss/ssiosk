import { motion } from "framer-motion";
import { styled } from "styled-components";

export const CommonContainer = styled(motion.div)`
  color: white;
  margin: 0;
  display: flex;
  justify-content: center;
  align-items: center;
`;
